package kindergarten;

public class Testing {
    
    

    public static void main(String[] args) {

        // SNode myCll = new SNode();
        // int len = 1;

        // SNode ptr = myCll;

        // for (int i = 0; i < len-1; i++) {
        //     ptr.setNext(new SNode());
        //     ptr = ptr.getNext();
        // }

        // ptr.setNext(myCll);

        // System.out.println(Classroom.cllLength(myCll));

        // Classroom.cllPopNth(myCll, 3);

        // System.out.println(Classroom.cllLength(myCll));

        // // for (int i = 0; i < 11; i++) {
        // //     Classroom.cllPopNth(myCll, 1);
        // //     System.out.println(Classroom.cllLength(myCll));
        // // }

    }

}
